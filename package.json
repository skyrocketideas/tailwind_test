{
	"name": "tailwind_test",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "parcel index.html --open",
		"clear": "rm -rf .cache && rm -rf dist",
		"build": "parcel build *.html --public-url ./",
		"tailwindbuild": "postcss tailwind.css -o public/build/tailwind.css",
		"build2": "rm -rf .cache && rm -rf dist && npm run build",
		"test": "echo \"Error: no test specified\" && exit 1",
		"devserver": "live-server --browser=Firefox"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"parcel-bundler": "^1.12.4"
	},
	"dependencies": {
		"autoprefixer": "^9.7.6",
		"postcss-cli": "^7.1.0",
		"tailwindcss": "^1.3.4"
	}
}
